# 情侣点餐小程序需求规格说明书

## 1. 文档信息

- 产品代号：Couple Order
- 目标平台：微信小程序
- 当前版本：MVP v1.0
- 文档状态：待产品、设计、研发评审

## 2. 产品概述

用户首次进入小程序时必须先完成身份确认，再通过建联建立唯一情侣关系。绑定成功后进入情侣首页，首页以几个大型生活功能模块作为入口；MVP 首先提供【给我做饭】。进入【给我做饭】后，用户才能使用菜单创建、点菜、下单、订单处理、验收和打赏能力。

核心原则：

1. 所有菜单、订单、打赏数据都必须绑定情侣关系，不以单个用户关系外的数据作为业务依据。
2. 男女角色只用于确认关系中的双方身份和展示，不代表权限高低；男方、女方能力对等。
3. 订单状态由菜单创建方推进，验收由下单方完成。

## 3. 目标与非目标

### 3.1 产品目标

- 用低成本方式确认情侣双方身份并完成绑定。
- 提供类似外卖平台的菜单浏览和下单体验。
- 让订单从创建到完成有清晰、可追踪的状态。
- 通过验收与打赏增强互动反馈。

### 3.2 MVP 非目标

- 不支持多人群组、多人拼单。
- 不支持公开菜单市场、陌生人浏览。
- 不支持复杂库存、配送、优惠券、退款体系。
- 不实现真实食材供应链管理，食材购买中仅表示订单处理阶段。

## 4. 用户角色与权限

| 角色 | 定义 | 核心权限 |
|---|---|---|
| 未建联用户 | 已登录但没有情侣关系 | 发起建联、接受建联、查看个人信息 |
| 男方 | 关系中的一方 | 创建/管理自己的菜单；浏览对方菜单；下单；验收；打赏 |
| 女方 | 关系中的另一方 | 创建/管理自己的菜单；浏览对方菜单；下单；验收；打赏 |

权限规则：

- 每个用户最多同时存在一个有效情侣关系。
- 关系双方均可创建多个菜单。
- 用户只能编辑、删除自己创建的菜单和菜品。
- 用户只能查看当前情侣关系下的菜单、订单和打赏。
- 订单创建后，订单快照独立保存；菜单或菜品后续修改不影响历史订单。
- 解除关系后，禁止新建订单；历史数据按产品配置保留，但双方默认不可继续操作。

## 5. 核心业务流程

### 5.1 初始化：登录、身份确认与状态分流

1. 用户进入小程序，完成微信身份识别。
2. 系统查询用户初始化状态：是否已确认角色、是否存在有效情侣关系。
3. 未确认角色：进入“身份确认页”，只能选择男方或女方，确认后写入用户档案。
4. 已确认角色但未建联：进入“情侣绑定引导页”，展示绑定说明和“发起绑定”“输入邀请码”两个入口。
5. 已发起但未完成绑定：进入“等待对方确认页”，展示邀请码、有效期和取消绑定入口。
6. 已存在有效关系：直接进入“情侣首页”。
7. 角色确认后默认不可自行修改；如需修改，必须解除关系并二次确认，MVP 可暂不开放修改。

初始化状态机：

`未登录` → `待确认身份` → `待建立关系` → `等待对方确认` → `已建立关系`

页面守卫：

- 未确认身份时，不允许进入绑定、首页和任何业务模块。
- 未建立关系时，不允许进入情侣首页和【给我做饭】内部页面。
- 绑定成功后，后续所有业务请求必须携带并校验 `relationshipId`。

### 5.2 建联绑定

微信分享链接必须进入独立邀请页，不复用初始化页。邀请页展示邀请方昵称、同意和拒绝按钮；接收方必须完成微信授权登录后才能处理邀请。接受后创建关系，拒绝后将结果写回邀请记录，并通过微信订阅消息通知发起方。

支持两种方式，MVP 至少实现邀请口令：

1. 用户 A 在绑定引导页点击“发起绑定”，生成一次性邀请码或邀请卡。
2. 用户 B 在绑定引导页点击“输入邀请码”，输入邀请码或扫描邀请卡。
3. 系统展示双方昵称、头像、角色和绑定确认页。
4. 用户 B 确认接受，系统校验双方角色不冲突且均无其他有效关系。
5. 绑定成功，生成 `relationshipId`，双方进入情侣首页。
6. 首页展示绑定成功反馈，例如“你们已经连在一起了”，并引导进入【给我做饭】。

异常：

- 口令过期、已使用、无效：提示并允许重新输入。
- 双方角色相同：禁止绑定并明确提示。
- 任一方已有有效关系：禁止重复建联。
- 用户取消确认：关系不创建。

### 5.3 情侣首页与功能模块

绑定成功后的首页不是菜单列表，而是情侣关系下的功能聚合入口。

MVP 首页内容：

1. 顶部关系卡片：展示双方头像、昵称、角色、相伴天数和关系状态。
2. 功能模块区：使用大卡片展示【给我做饭】入口，包含简短描述和主行动按钮。
3. 最近动态区：展示最近订单状态、验收提醒或打赏结果。
4. 空状态：暂无订单时展示“给对方点一份心意”引导。

【给我做饭】模块入口规则：

- 点击首页大卡片进入模块首页。
- 模块首页展示“点菜给 TA”和“为 TA 做菜单”两个主要操作。
- 菜单、点菜、购物车和订单详情均属于该模块的二级页面，不直接平铺在情侣首页。
- 后续新增功能模块（如纪念日、心愿单）沿用首页大卡片结构，不改变首页信息架构。

### 5.4 菜单创建

1. 用户从情侣首页进入【给我做饭】，再进入“为 TA 做菜单”。
2. 创建菜单，填写菜单名称、简介、封面图、菜品分类。
3. 在分类下新增菜品，填写菜品名称、图片、描述、份量/规格、预计制作时间、建议打赏金额。
4. 菜单可保存为草稿或发布。
5. 发布后的菜单出现在对方“点菜”页面。
6. 用户可编辑、上下架、删除自己的菜单和菜品。

### 5.5 对方点菜与下单

1. 用户从情侣首页进入【给我做饭】，再点击“点菜给 TA”，查看对方已发布菜单。
2. 页面采用外卖平台形态：左侧分类标签，右侧菜品列表。
3. 点击菜品查看详情，选择数量及备注后加入购物车。
4. 购物车展示菜品、数量、备注、预计制作时间和可选打赏金额。
5. 提交订单前展示订单确认页。
6. 下单成功后，菜单创建方收到小程序订阅消息或站内提醒。

### 5.6 订单处理与验收

订单状态必须按以下顺序推进：

`创建` → `食材购买中` → `制作中` → `验收` → `已完成`

- 菜单创建方可将订单从“创建”推进到“食材购买中”。
- 菜单创建方可将订单从“食材购买中”推进到“制作中”。
- 菜单创建方可将订单从“制作中”推进到“验收”。
- 下单方在“验收”状态可确认“满意并完成”，订单变为“已完成”。
- 下单方可在“验收”状态提出“需要调整”，订单退回“制作中”，并填写反馈。
- 每次状态变更记录操作人、时间和备注。
- MVP 不支持任意跳转、撤回或删除订单；如需取消，单独增加“取消订单”能力并限制在“创建”状态。

### 5.7 打赏

- 打赏对象为本次订单的菜单创建方。
- 打赏入口至少出现在“验收”页和“订单详情页”。
- 支持预设金额和自定义金额，金额必须大于 0。
- 打赏与订单一对一关联，允许订单完成前发起一次；支付失败可重试。
- 支付成功后记录金额、支付状态、支付时间和支付流水号。
- MVP 可先实现“打赏意愿/金额记录” mock 流程；正式上线必须接入微信支付并完成合规配置。

## 6. 页面与导航

### 6.1 页面清单

| 页面 | 类型 | 主要内容 |
|---|---|---|
| 登录/欢迎页 | 初始化页 | 微信身份识别、产品介绍、初始化状态查询 |
| 身份确认页 | 初始化页 | 男方/女方选择、确认说明 |
| 情侣绑定引导页 | 初始化页 | 发起绑定、输入邀请码、扫码入口 |
| 等待对方确认页 | 初始化页 | 邀请码、有效期、等待状态 |
| 绑定成功页 | 初始化页 | 双方信息、关系建立反馈、进入首页 |
| 情侣首页 | TabBar | 关系卡片、功能模块、最近动态 |
| 给我做饭模块首页 | 二级页 | 点菜给 TA、为 TA 做菜单、模块订单摘要 |
| 点菜页 | 模块二级页 | 左侧分类、右侧菜品、购物车入口 |
| 订单页 | TabBar | 我发起的订单、我收到的订单、状态筛选 |
| 我的菜单页 | 模块二级页 | 菜单列表、发布/编辑入口 |
| 菜单编辑页 | 二级页 | 菜单信息、分类和菜品维护 |
| 菜品详情页 | 二级页 | 图片、描述、规格、加入购物车 |
| 购物车/确认订单页 | 二级页 | 明细、备注、打赏、提交订单 |
| 订单详情页 | 二级页 | 状态时间线、操作按钮、反馈与打赏 |
| 关系设置页 | 二级页 | 对方信息、解除关系 |
| 打赏结果页 | 二级页 | 支付结果、金额、感谢反馈 |

### 6.2 TabBar 建议

1. 情侣主页
2. 订单
3. 我的

说明：点菜和我的菜单从【给我做饭】模块进入，不作为一级 TabBar，避免首页功能过度分散。

### 6.3 页面布局要求

#### 初始化页面

- 页面采用单一主任务布局，每一步只突出一个核心动作。
- 身份确认页：顶部说明“先告诉我们你是谁”，中部提供男方、女方两个大尺寸互斥选项，底部固定“确认身份”按钮。
- 情侣绑定引导页：展示当前身份和绑定说明，提供“发起绑定”和“输入邀请码”两个同等重要的入口。
- 等待确认页：突出邀请码、复制按钮、有效期和“重新生成/取消绑定”操作。
- 绑定成功页：展示双方头像和连接反馈，底部仅保留“进入首页”主按钮。

#### 情侣首页

- 首页采用“大卡片功能入口”布局，不直接展示菜单分类和菜品列表。
- 顶部为关系卡片，突出双方信息和当前关系状态。
- 中部为功能模块卡片；MVP 只展示一个大模块【给我做饭】，卡片需包含模块图形、标题、描述和进入按钮。
- 底部为最近动态或订单摘要，不抢占功能模块的主视觉。
- 无关系时不得渲染首页内容，必须被页面守卫重定向到初始化流程。

#### 给我做饭模块

- 模块首页提供两个主入口：“点菜给 TA”和“为 TA 做菜单”。
- 点菜页延续外卖平台布局：左侧固定分类栏，右侧纵向菜品列表，底部固定点菜篮。
- 菜单管理页突出“已发布/草稿”状态和“添加菜品”操作。

## 7. 关键交互与校验

### 7.1 菜单与菜品

- 菜单名称：必填，2-30 字。
- 菜品名称：必填，1-30 字。
- 菜品描述：选填，最多 200 字。
- 图片：菜品至少 1 张，限制格式和大小，上传失败可重试。
- 菜单无已发布菜品时不可发布。
- 下架菜单不可被新订单选中，但不影响历史订单。

### 7.2 订单

- 购物车只能包含同一菜单创建方的菜品。
- 菜单或菜品下架后，已加入购物车的内容在提交时必须重新校验。
- 下单时保存菜品名称、图片、描述、数量、备注等快照。
- 防止重复提交：提交按钮进入 loading，服务端使用幂等键。
- 网络失败时明确提示，不得展示“下单成功”。

### 7.3 打赏

- 金额精确到分，最小金额由支付规则配置。
- 支付中禁止重复发起支付。
- 支付成功以服务端回调为准，客户端结果仅用于展示。
- 打赏失败不改变订单状态。

## 8. 数据模型

### 8.1 `users`

`_openid`, `nickname`, `avatar`, `genderRole`, `onboardingStatus`, `pendingInviteId`, `status`, `createTime`, `updateTime`

`onboardingStatus` 枚举：`pending_role`、`pending_relationship`、`waiting_confirmation`、`completed`

### 8.2 `relationships`

`_id`, `memberOpenids`, `memberRoles`, `status`, `inviteCodeHash`, `inviteExpireTime`, `createTime`, `dissolveTime`

索引：`memberOpenids`, `status`

### 8.3 `menus`

`_id`, `relationshipId`, `ownerOpenid`, `name`, `description`, `coverUrl`, `status`, `publishedAt`, `createTime`, `updateTime`

索引：`relationshipId`, `ownerOpenid`, `status`

### 8.4 `menu_categories`

`_id`, `menuId`, `name`, `sort`, `status`, `createTime`

### 8.5 `dishes`

`_id`, `menuId`, `categoryId`, `name`, `imageUrls`, `description`, `specification`, `prepMinutes`, `tipSuggestedAmount`, `status`, `sort`, `createTime`, `updateTime`

索引：`menuId`, `categoryId`, `status`

### 8.6 `orders`

`_id`, `relationshipId`, `buyerOpenid`, `sellerOpenid`, `menuId`, `status`, `itemsSnapshot`, `buyerRemark`, `tipAmount`, `tipStatus`, `totalDishCount`, `createdAt`, `updatedAt`, `completedAt`

索引：`relationshipId`, `buyerOpenid`, `sellerOpenid`, `status`, `createdAt`

### 8.7 `order_status_logs`

`_id`, `orderId`, `fromStatus`, `toStatus`, `operatorOpenid`, `remark`, `createTime`

### 8.8 `tips`

`_id`, `relationshipId`, `orderId`, `payerOpenid`, `receiverOpenid`, `amount`, `status`, `transactionId`, `paidAt`, `createTime`

唯一约束建议：`orderId + payerOpenid`

### 8.9 `couple_states`

用于 MVP 阶段保存当前用户可访问的情侣业务快照，服务端通过 `_openid` 识别用户并限制读取范围。后续数据规模扩大后，可将快照拆分为前述标准业务集合。

`_openid`, `snapshot`, `createTime`, `updateTime`

索引：`_openid`

### 8.10 `relationships`

情侣关系主表，所有菜单、订单、纪念日、查岗和热度数据必须通过 `relationshipId` 归属到该表，不允许按用户各自保存完整业务快照。

`_id`, `memberOpenids`, `roles`, `status`, `linkedAt`, `createTime`

索引：`memberOpenids`, `status`

### 8.11 `relationship_invites`

微信分享建联邀请，使用一次性 token，服务端校验状态、有效期、邀请方和角色冲突。

`token`, `inviterOpenid`, `inviterRole`, `status`, `acceptedBy`, `relationshipId`, `createTime`, `acceptedAt`

索引：`token`, `status`

## 9. 后端接口/云函数建议

| 函数 | 用途 |
|---|---|
| `login` | 获取用户身份、读取初始化状态并返回首页分流结果 |
| `confirmRole` | 保存男方/女方角色 |
| `createRelationshipInvite` | 创建一次性建联口令 |
| `joinRelationship` | 校验口令并建立关系 |
| `getRelationship` | 获取当前情侣关系 |
| `dissolveRelationship` | 解除关系 |
| `createMenu` / `updateMenu` / `publishMenu` | 菜单 CRUD 和发布 |
| `getPartnerMenus` | 获取对方可见菜单 |
| `createDish` / `updateDish` / `deleteDish` | 菜品维护 |
| `createOrder` | 创建订单并执行关系、菜单、幂等校验 |
| `getOrders` | 查询我发起/收到的订单 |
| `updateOrderStatus` | 按状态机推进订单 |
| `submitAcceptance` | 验收通过或退回制作 |
| `createTipPayment` | 创建打赏支付 |
| `handleTipPaymentCallback` | 接收支付结果并更新打赏状态 |
| `getCoupleState` | 获取当前用户的情侣业务快照 |
| `syncCoupleState` | 将当前关系下的前端状态同步到数据库 |
| `createInvite` | 创建一次性微信分享邀请 |
| `acceptInvite` | 校验并接受微信分享邀请，创建情侣关系 |
| `sendCheckInNotification` | 向关系另一方发送查岗订阅消息 |
| `sendOrderNotification` | 向做饭方发送新订单订阅消息 |
| `sendAnniversaryReminder` | 在纪念日前 5 天向双方发送提醒 |

所有接口必须从服务端身份获取当前用户，不信任客户端传入的 `openid`、角色或 `relationshipId`。所有关系内数据访问必须校验当前用户属于该关系。

### 9.1 云端存储策略

- 微信小程序启动时调用 `login` 获取并初始化用户记录。
- 微信环境优先从 `couple_states` 恢复状态；页面和业务操作产生的状态变更自动同步。
- H5、抖音和支付宝预览使用 mock 数据，不访问云端。
- 云函数部署前需要配置真实 CloudBase 环境 ID；当前代码使用空环境占位。

### 9.2 查岗与定位通知

- 发起方在情侣关系内创建查岗请求。
- 接收方收到微信订阅消息后，可选择“同意并发送定位”或“暂不分享”。
- 同意后仅上传一次当前定位，发起方可查看位置、地址和更新时间，不建立持续定位。
- 前端通过 `requestSubscribeMessage` 请求订阅授权，服务端云函数 `sendCheckInNotification` 负责向另一半的 `openid` 发送通知。
- 上线前需将 `CHECK_IN_TEMPLATE_ID` 配置为微信公众平台审核通过的订阅消息模板 ID，并完善关系双方 `openid` 映射。

## 10. 通知

- 建联成功：双方收到站内通知。
- 新订单：菜单创建方收到通知。
- 订单状态变更：下单方收到通知。
- 进入验收：下单方收到重点提醒。
- 打赏成功：菜单创建方收到通知。

微信订阅消息授权被拒绝时，站内消息仍应可用，不能阻塞核心业务。

## 11. 非功能要求

- 适配微信小程序常见屏幕尺寸，避免横向溢出。
- 菜品图片使用固定比例容器和裁剪模式，避免列表布局跳动。
- 核心操作提供 loading、成功、失败和空状态。
- 订单状态变更、支付回调、关系建立必须具备服务端日志。
- 敏感数据最小化存储；邀请口令不得明文持久化。
- 数据库默认仅关系成员可读写，菜单只能由创建者修改。
- 所有金额使用整数分存储，禁止使用浮点数参与结算。
- 关键接口需要幂等和并发控制，尤其是建联、下单、状态更新、支付回调。

## 12. 验收标准

### 12.1 建联

- 新用户首次进入时必须先完成身份确认，未确认身份不得看到绑定和业务页面。
- 已确认身份但未建联用户必须进入绑定引导页，不能直接进入情侣首页。
- 绑定成功后双方进入情侣首页，并看到【给我做饭】大功能入口。
- 两个不同角色的微信用户可以通过有效口令成功建立关系。
- 相同角色、过期口令、重复建联均被阻止并显示明确原因。
- 未建联用户不能进入情侣首页、【给我做饭】、点菜、订单和菜单管理核心页面。

### 12.2 菜单与点菜

- 情侣首页默认不展示菜品列表，点击【给我做饭】后才进入菜单相关功能。
- 【给我做饭】模块首页必须同时提供“点菜给 TA”和“为 TA 做菜单”两个入口。
- 男方和女方均可创建并发布自己的菜单。
- 对方能看到已发布菜单，不能看到草稿或已下架菜品。
- 点菜页具备左侧分类、右侧菜品列表、菜品详情、购物车和备注。
- 菜品下架或菜单变更后，下单提交会重新校验并给出提示。

### 12.3 订单

- 下单成功后，双方均能在各自订单列表看到正确的订单。
- 订单状态只能按规定顺序变更，非法状态跳转被拒绝。
- 下单方可以在验收阶段通过或退回制作。
- 订单详情展示状态时间线和每次操作记录。

### 12.4 打赏

- 用户可在验收阶段发起打赏。
- 支付成功后订单和打赏记录关联正确，失败可重试且不重复扣款。
- 菜单创建方能收到打赏成功通知。

## 13. 待确认事项

1. “男方/女方”是否必须严格二选一，还是允许用户自定义称谓？
2. 建联方式是否只做口令，还是首版必须支持二维码/邀请卡？
3. 订单是否允许取消、拒单、超时关闭和重新下单？
4. 是否需要真实微信支付，还是 MVP 先采用模拟打赏？
5. 打赏是否允许多次追加，还是每个订单只允许一次？
6. 是否需要上传制作完成照片作为验收凭证？
7. 解除关系后历史订单和打赏记录的保留、导出与可见范围。
8. 是否需要语音/图片备注，以及是否限制内容审核。

## 14. MVP 开发优先级

### P0

- 微信登录、初始化状态分流、角色确认、邀请口令建联
- 绑定成功页、情侣首页、【给我做饭】模块首页
- 菜单/分类/菜品创建与发布
- 对方点菜、购物车、下单
- 订单状态机、订单详情、验收
- 站内通知

### P1

- 微信支付打赏
- 订阅消息
- 二维码邀请
- 制作完成照片
- 订单取消与异常处理

### P2

- 菜单模板复制
- 常点菜品
- 纪念日/订单统计
- 个性化角色称谓与主题
